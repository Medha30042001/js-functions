/*
Q: 15
Password Strength Checker
Write a function named checkPasswordStrength that evaluates the strength of a given password.

The function should take one parameter:

password (a string): The password to evaluate.
Return "Weak" if the password:

Is less than 6 characters
Return "Moderate" if the password:

Is 6 characters or more but contains only letters or only numbers.
Return "Strong" if the password:

Is 6 characters or more and contains a mix of letters, numbers, and special characters.
Example:

checkPasswordStrength("12345");       // Should return "Weak"
checkPasswordStrength("password");   // Should return "Moderate"
checkPasswordStrength("Pass123!");   // Should return "Strong"
Submission Guidelines
Once Completed, submit the code directly in the box below
*/
